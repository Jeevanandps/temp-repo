git init -> for initializing git in pwd

git config --global user.name ""  -> for adding username
git config --global user.email ""  -> for adding email

git status -> list all untracked files

git add -A -> adding all changes to staging area

git commit -m" "  -> adding staged files to local repo

git remote add origin https://github.com/Jeevanandps/python.git

git remote -v

git branch -> [list all branches]

git branch branch_name  ->[create a new branch]

git checkout branch_name  ->[to checkout to another branch]

git merge branch_name  ->[merges all changes from a branch to master branch]

  this line is added to student branch

this line is added from feature branch